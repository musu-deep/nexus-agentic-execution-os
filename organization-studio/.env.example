# Leave empty when Organization Studio is served behind the same origin as NEXUS.
VITE_NEXUS_API_URL=

# Optional local development proxy target used by vite.config.ts.
NEXUS_PROXY_TARGET=http://127.0.0.1:8000
